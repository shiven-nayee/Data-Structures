public class Tester {
  public static void main(String[] args) {
    Graph graph = new Graph();
    graph.addVertice(0);
    graph.addVertice(1);
    graph.addVertice(2);
    graph.addVertice(3);
    graph.addVertice(4);
    System.out.println("Graph is: " + graph);
  }
}